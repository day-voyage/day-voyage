import {expect} from 'chai'
import CONSTANTS from '../../src/constants';
import activities from '../../src/reducers/activities'

describe('dbactivities_reducer_spec', () => {

  it('handles RECEIVE_DBACTIVITIES', () => {
    // const initialState = [];
    // const action = {type: CONSTANTS.RECEIVE_ACTIVITIES, activities: [{city: 'San Francisco', neighborhood: 'Soma'}]};
    // const nextState = activities(initialState, action);
    // expect(nextState).to.deep.equal([{city: 'San Francisco', neighborhood: 'Soma'}]);
  });

  it('handles DB_ADD_TO_BUILDER', () => {
    // const initialState = [];
    // const action = {type: CONSTANTS.RECEIVE_ACTIVITIES, activities: [{city: 'San Francisco', neighborhood: 'Soma'}]};
    // const nextState = activities(initialState, action);
    // expect(nextState).to.deep.equal([{city: 'San Francisco', neighborhood: 'Soma'}]);
  });

  it('handles DB_DELETE_FROM_BUILDER', () => {
    // const initialState = [];
    // const action = {type: CONSTANTS.RECEIVE_ACTIVITIES, activities: [{city: 'San Francisco', neighborhood: 'Soma'}]};
    // const nextState = activities(initialState, action);
    // expect(nextState).to.deep.equal([{city: 'San Francisco', neighborhood: 'Soma'}]);
  });

  it('handles DB_CHECK_NEIGHBORHOOD', () => {
    // const initialState = [];
    // const action = {type: CONSTANTS.RECEIVE_ACTIVITIES, activities: [{city: 'San Francisco', neighborhood: 'Soma'}]};
    // const nextState = activities(initialState, action);
    // expect(nextState).to.deep.equal([{city: 'San Francisco', neighborhood: 'Soma'}]);
  });

  it('handles DB_CHECK_CATEGORY', () => {
    // const initialState = [];
    // const action = {type: CONSTANTS.RECEIVE_ACTIVITIES, activities: [{city: 'San Francisco', neighborhood: 'Soma'}]};
    // const nextState = activities(initialState, action);
    // expect(nextState).to.deep.equal([{city: 'San Francisco', neighborhood: 'Soma'}]);
  });

  it('handles DB_CHECK_BUDGET', () => {
    // const initialState = [];
    // const action = {type: CONSTANTS.RECEIVE_ACTIVITIES, activities: [{city: 'San Francisco', neighborhood: 'Soma'}]};
    // const nextState = activities(initialState, action);
    // expect(nextState).to.deep.equal([{city: 'San Francisco', neighborhood: 'Soma'}]);
  });



})